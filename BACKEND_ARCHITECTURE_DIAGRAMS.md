# 🎨 Backend Architecture Visual Guide

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             MOBILE APP (CLIENT)                             │
│                                                                             │
│  ┌───────────────────┐  ┌──────────────────┐  ┌─────────────────────────┐  │
│  │ EphemerisService  │  │ AIReflectionSvc  │  │  Other Services         │  │
│  │                   │  │                  │  │  (Transits, Qibla, etc) │  │
│  │ • Feature flags   │  │ • Secure routing │  │                         │  │
│  │ • Auto fallback   │  │ • Error handling │  │                         │  │
│  │ • Local cache     │  │ • Multi-language │  │                         │  │
│  └────────┬──────────┘  └────────┬─────────┘  └─────────────────────────┘  │
│           │                      │                                          │
│           └──────────┬───────────┘                                          │
│                      │                                                      │
│            ┌─────────▼─────────┐                                            │
│            │ EdgeFunctionClient │                                           │
│            │ • Retry logic      │                                           │
│            │ • Timeout handling │                                           │
│            │ • Perf logging     │                                           │
│            └─────────┬─────────┘                                            │
└──────────────────────┼──────────────────────────────────────────────────────┘
                       │
                       │ HTTPS Request
                       │ Authorization: Bearer {ANON_KEY}
                       │
┌──────────────────────▼──────────────────────────────────────────────────────┐
│                        SUPABASE EDGE FUNCTIONS                              │
│                                                                             │
│  ┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────────┐  │
│  │  ephemeris/          │  │  ai-reflection/      │  │  precompute-     │  │
│  │  index.ts            │  │  index.ts            │  │  ephemeris/      │  │
│  │                      │  │                      │  │  index.ts        │  │
│  │  ┌────────────────┐  │  │  ┌────────────────┐  │  │                  │  │
│  │  │ 1. Check cache │  │  │  │ 1. Check cache │  │  │  Cron Schedule:  │  │
│  │  │ 2. NASA API    │  │  │  │ 2. Call Groq   │  │  │  Every hour :05  │  │
│  │  │ 3. Store cache │  │  │  │ 3. Store cache │  │  │                  │  │
│  │  │ 4. Log metrics │  │  │  │ 4. Log metrics │  │  │  Pre-fills 48h   │  │
│  │  └────────────────┘  │  │  └────────────────┘  │  │  of planet data  │  │
│  │                      │  │                      │  │                  │  │
│  │  Response: <100ms    │  │  Response: <2s       │  │  336 entries/run │  │
│  │  (95%+ cache hit)    │  │  (cached in 5min)    │  │                  │  │
│  └──────────┬───────────┘  └──────────┬───────────┘  └──────────┬───────┘  │
└─────────────┼──────────────────────────┼──────────────────────────┼──────────┘
              │                          │                          │
              │ Database Queries         │                          │
              │                          │                          │
┌─────────────▼──────────────────────────▼──────────────────────────▼──────────┐
│                       SUPABASE POSTGRESQL DATABASE                          │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                        CACHE TABLES                                  │   │
│  ├──────────────────┬──────────────────┬──────────────────┬────────────┤   │
│  │ ephemeris_cache  │ transit_cache    │ ai_response_cache│ api_call_  │   │
│  │                  │                  │                  │ metrics    │   │
│  │ 336 rows         │ 2,000 rows       │ 1,000 rows       │ 30d log    │   │
│  │ 48h TTL          │ 5-30min TTL      │ 5min TTL         │            │   │
│  │ ~10 MB           │ ~5 MB            │ ~5 MB            │ ~10 MB     │   │
│  └──────────────────┴──────────────────┴──────────────────┴────────────┘   │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                      POSTGRES FUNCTIONS                              │   │
│  ├───────────────────────┬──────────────────────┬───────────────────────┤   │
│  │ cleanup_expired_cache │ get_cache_stats()    │ check_api_health()    │   │
│  │ (Daily 3 AM UTC)      │ (On-demand)          │ (On-demand)           │   │
│  └───────────────────────┴──────────────────────┴───────────────────────┘   │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                      CRON JOBS (pg_cron)                             │   │
│  ├────────────────────────────────┬─────────────────────────────────────┤   │
│  │ precompute-ephemeris-hourly    │ cleanup-expired-cache-daily         │   │
│  │ Schedule: 5 * * * *             │ Schedule: 0 3 * * *                 │   │
│  │ Calls Edge Function             │ Calls cleanup_expired_cache()       │   │
│  └────────────────────────────────┴─────────────────────────────────────┘   │
└─────────────┬─────────────────────────────┬──────────────────────────────────┘
              │                             │
              │ External API Calls          │
              │ (<100/day vs 7,000/day)     │
              │                             │
┌─────────────▼────────────┐   ┌────────────▼──────────┐
│   NASA JPL HORIZONS API  │   │     GROQ API          │
│                          │   │     (LLaMA 3.3)       │
│   • Planetary positions  │   │                       │
│   • Public (no auth)     │   │   • AI text rewriting │
│   • Rate limited         │   │   • Key server-side   │
│   • 2-3s latency         │   │   • <2s response      │
└──────────────────────────┘   └───────────────────────┘
```

---

## Data Flow Diagrams

### 1. Ephemeris Request Flow (95% Cache Hit)

```
User Opens App
     │
     ▼
Request Planet Position
     │
     ├─────────────────────────────────────┐
     │                                     │
     ▼                                     ▼
Edge Function                         Feature Flag ON?
     │                                     │
     ▼                                     └─→ NO → Direct NASA API (Fallback)
Check Database Cache
     │
     ├───────────────────┐
     │                   │
     ▼                   ▼
Cache HIT (95%)     Cache MISS (5%)
     │                   │
     │                   ▼
     │              Fetch from NASA
     │                   │
     │                   ▼
     │              Store in Database
     │                   │
     └───────────┬───────┘
                 │
                 ▼
           Log Metrics
                 │
                 ▼
         Return to App (<100ms)
                 │
                 ▼
         Display Transit Data
```

### 2. AI Reflection Flow (Security Fixed)

```
User Enables AI Rewriting
     │
     ▼
Send Text to Edge Function
     │
     ▼
Edge Function (Server-Side)
     │
     ├─────────────────────────┐
     │                         │
     ▼                         ▼
Check 5min Cache          Feature Flag ON?
     │                         │
     ├──────────┐              └─→ NO → Return Original
     │          │
     ▼          ▼
Cache HIT  Cache MISS
     │          │
     │          ▼
     │     Call Groq API
     │     (Key: Server-Side)
     │          │
     │          ▼
     │     Store in Cache
     │          │
     └────┬─────┘
          │
          ▼
    Return Rewritten Text
          │
          ▼
    Display to User
```

### 3. Pre-computation Cron Flow

```
Every Hour at :05
     │
     ▼
Cron Trigger
     │
     ▼
Precompute Edge Function
     │
     ▼
For Each Hour (0-47):
     │
     ├─→ For Each Planet (7):
     │        │
     │        ▼
     │   Check if Already Cached?
     │        │
     │        ├───────────┐
     │        │           │
     │        ▼           ▼
     │     YES (Skip)   NO (Fetch)
     │                     │
     │                     ▼
     │              Fetch from NASA
     │                     │
     │                     ▼
     │              Store in Database
     │                     │
     └─────────────────────┘
               │
               ▼
     Results: 336 cached, 0 skipped (first run)
               │
               ▼
     Next Hour: 0 cached, 336 skipped (already done)
```

---

## Performance Visualization

### Response Time Comparison

```
Before (Direct NASA API):
[████████████████████████████████████████] 2-3 seconds

After (Database Cache):
[██] <100ms (95% of requests)
[████████████████] <3s (5% cache misses)
```

### API Call Reduction

```
Before: 1,000 users × 7 planets/day = 7,000 NASA API calls/day
██████████████████████████████████████████████████ 7,000 calls

After: Pre-computation + Cache = <100 NASA API calls/day
█ <100 calls (99% reduction)
```

### Cache Hit Rate Timeline

```
Hour 0:   [░░░░░░░░░░] 0% (just deployed)
Hour 1:   [████████░░] 80% (initial pre-computation)
Hour 2:   [█████████░] 90% (more cache hits)
Hour 24:  [██████████] 95%+ (stable)
Hour 48+: [██████████] 99%+ (optimal)
```

---

## Database Schema Visualization

```
┌─────────────────────────────────────────────────────────────┐
│                    ephemeris_cache                          │
├────────────────┬────────────┬──────────────┬────────────────┤
│ id (UUID)      │ planet_id  │ date_hour    │ longitude      │
│ expires_at     │ cached_at  │ ...          │                │
├────────────────┴────────────┴──────────────┴────────────────┤
│ Indexes:                                                    │
│ • idx_ephemeris_planet_date (planet_id, date_hour)          │
│ • idx_ephemeris_expires (expires_at)                        │
│ • idx_ephemeris_unique (planet_id, date_hour) UNIQUE        │
└─────────────────────────────────────────────────────────────┘
           │
           │ Foreign key (conceptual)
           ▼
┌─────────────────────────────────────────────────────────────┐
│                    api_call_metrics                         │
├────────────────┬────────────┬──────────────┬────────────────┤
│ id (UUID)      │ endpoint   │ cache_hit    │ response_time  │
│ status_code    │ created_at │ ...          │                │
├────────────────┴────────────┴──────────────┴────────────────┤
│ Indexes:                                                    │
│ • idx_metrics_endpoint_time (endpoint, created_at DESC)     │
│ • idx_metrics_cache (cache_hit, cache_source)               │
└─────────────────────────────────────────────────────────────┘
```

---

## Monitoring Dashboard Layout

```
┌─────────────────────────────────────────────────────────────┐
│                    ASRĀRIYA BACKEND DASHBOARD               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────┐  ┌──────────────────────┐        │
│  │ Cache Hit Rate       │  │ Response Time        │        │
│  │                      │  │                      │        │
│  │      95.8%           │  │      78ms (avg)      │        │
│  │  ██████████ 95%+     │  │  Target: <100ms      │        │
│  │                      │  │  ✅ PASSING          │        │
│  └──────────────────────┘  └──────────────────────┘        │
│                                                             │
│  ┌──────────────────────┐  ┌──────────────────────┐        │
│  │ API Calls (24h)      │  │ Database Size        │        │
│  │                      │  │                      │        │
│  │  Total: 5,420        │  │      28.4 MB         │        │
│  │  Cached: 5,193       │  │  Quota: 50 MB        │        │
│  │  NASA: 227           │  │  Usage: 56.8%        │        │
│  │  (96% reduction)     │  │  ✅ HEALTHY          │        │
│  └──────────────────────┘  └──────────────────────┘        │
│                                                             │
│  ┌───────────────────────────────────────────────┐         │
│  │ Error Rate (Last Hour)                        │         │
│  │                                               │         │
│  │  Ephemeris: 0.2% (1/500)   ✅                │         │
│  │  AI Reflect: 0.0% (0/120)  ✅                │         │
│  │  Precompute: 0.0% (0/1)    ✅                │         │
│  │                                               │         │
│  │  Target: <1%  ✅ ALL PASSING                 │         │
│  └───────────────────────────────────────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────┐         │
│  │ Active Alerts                                 │         │
│  │                                               │         │
│  │  ✅ No active alerts                         │         │
│  │                                               │         │
│  └───────────────────────────────────────────────┘         │
│                                                             │
└─────────────────────────────────────────────────────────────┘

Last Updated: 2026-01-27 14:35:00 UTC
Auto-refresh: Every 5 minutes
```

---

## Deployment Timeline Visualization

```
Week 1: Initial Deployment
├─ Day 1: Deploy database + functions
│  ├─ supabase db push              [████████] Complete
│  ├─ supabase functions deploy     [████████] Complete
│  └─ Setup cron jobs               [████████] Complete
│
├─ Day 2: 10% Rollout
│  ├─ Set rollout to 10%            [████████] Complete
│  └─ Monitor metrics (24h)         [████░░░░] In Progress
│
├─ Day 3: 50% Rollout
│  ├─ Set rollout to 50%            [░░░░░░░░] Pending
│  └─ Monitor metrics (24h)         [░░░░░░░░] Pending
│
└─ Day 4-7: 100% Rollout
   ├─ Set rollout to 100%           [░░░░░░░░] Pending
   ├─ Monitor metrics daily         [░░░░░░░░] Pending
   └─ Performance tuning            [░░░░░░░░] Pending

Week 2: Security Hardening
├─ Remove client API key            [░░░░░░░░] Pending
├─ Rebuild app for stores           [░░░░░░░░] Pending
└─ Submit to app stores             [░░░░░░░░] Pending

Week 3+: Enhancement
├─ Add more cache features          [░░░░░░░░] Pending
├─ Automated alerting               [░░░░░░░░] Pending
└─ Cost optimization                [░░░░░░░░] Pending
```

---

## Cost Comparison Visualization

```
BEFORE (Direct API Calls):
├─ NASA API: 7,000 calls/day × $0 (free but rate limited)
├─ Groq API: 500 calls/day × $0.0001 = $0.05/day
└─ Total: ~$1.50/month (risk of rate limit blocks)

AFTER (Cached Backend):
├─ NASA API: <100 calls/day × $0 = $0
├─ Groq API: 200 calls/day × $0.0001 = $0.02/day (60% reduction)
├─ Supabase Database: 30 MB storage = Free (included)
├─ Supabase Edge Functions: <1M invocations = Free (included)
└─ Total: ~$0.60/month (60% cost reduction + no rate limits)

SAVINGS: ~$1.00/month × 12 = $12/year
RISK REDUCTION: No rate limit blocks = Priceless
```

---

**Last Updated:** January 27, 2026  
**Version:** 1.0.0
